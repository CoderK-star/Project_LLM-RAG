{
  "name": "gomi-collection-assistant",
  "version": "2.0.0",
  "description": "ごみ収集アシスタント - AIでごみの分別をサポート (PWA)",
  "private": true,
  "scripts": {
    "dev": "cd backend && uvicorn app:app --host 0.0.0.0 --port 8000 --reload"
  }
}
